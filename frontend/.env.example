# URL of the deployed backend
VITE_SERVER_URL=http://localhost:5000

# Note: there's no PIN env var here anymore. PINs are now per-clinic,
# set when a clinic is created via the landing page ("/"), and checked
# server-side - not a single global frontend constant.
